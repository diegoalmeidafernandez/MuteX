<Application x:Class="MuteX.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">

    <Application.Resources>

        <!-- ===== PALETA ESTILO WINDOWS 11 ===== -->
        <SolidColorBrush x:Key="AppBackground" Color="#FAFAFC" />
        <SolidColorBrush x:Key="ButtonBackground" Color="#E5E5EA" />
        <SolidColorBrush x:Key="ButtonBorder" Color="#C8C8CC" />
        <SolidColorBrush x:Key="TextColorPrimary" Color="#222" />
        <SolidColorBrush x:Key="TextColorSecondary" Color="#444" />

        <!-- ===== ESTILO GLOBAL DE BOTONES ===== -->
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI Variable" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Foreground" Value="{StaticResource TextColorPrimary}" />
            <Setter Property="Background" Value="{StaticResource ButtonBackground}" />
            <Setter Property="BorderBrush" Value="{StaticResource ButtonBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="Margin" Value="0,5" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Cursor" Value="Hand" />

            <!-- Botones redondeados -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== TEXTBLOCK GLOBAL ===== -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI Variable" />
            <Setter Property="Foreground" Value="{StaticResource TextColorPrimary}" />
        </Style>

        <!-- ===== CHECKBOX GLOBAL ===== -->
        <Style TargetType="CheckBox">
            <Setter Property="FontFamily" Value="Segoe UI Variable" />
            <Setter Property="Foreground" Value="{StaticResource TextColorPrimary}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Margin" Value="0,6,0,0" />
        </Style>

        <!-- ===== ESTILO DE TODAS LAS VENTANAS ===== -->
        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="Segoe UI Variable" />
            <Setter Property="Background" Value="{StaticResource AppBackground}" />
        </Style>

    </Application.Resources>
</Application>
